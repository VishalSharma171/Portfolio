/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // configure domains if you pull images from remote sources
    domains: []
  }
};
module.exports = nextConfig;
